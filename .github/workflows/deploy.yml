name: ByteFerry CI/CD

on:
  push:
    branches: [ "main", "master" ]
    tags: [ "v*", "release-*" ]
  workflow_dispatch: {}

env:
  REGISTRY: docker.io
  IMAGE_NAME: haibaraiii/byteferry
  CONTAINER_NAME: byteferry
  SSH_HOST: 154.94.235.178
  SSH_USER: root
  APP_PORT: "8076"
  VOLUME_DATA_DIR: /root/.byteferry/files

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Build with Maven
        run: mvn -B -DskipTests package

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Decide SSH auth method
        id: decide
        run: |
          if [ -n "${{ secrets.SSH_PASSWORD }}" ]; then
            echo "use_password=true" >> "$GITHUB_OUTPUT"
          else
            echo "use_password=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Check SSH_PRIVATE_KEY exists
        if: ${{ steps.decide.outputs.use_password == 'false' }}
        run: |
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "SSH_PRIVATE_KEY is not set"
            exit 1
          else
            echo "SSH_PRIVATE_KEY is set"
          fi

      - name: Start ssh-agent and add private key
        if: ${{ steps.decide.outputs.use_password == 'false' }}
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ env.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Remote deploy over SSH
        if: ${{ steps.decide.outputs.use_password == 'false' }}
        env:
          REGISTRY: ${{ env.REGISTRY }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          CONTAINER_NAME: ${{ env.CONTAINER_NAME }}
          APP_PORT: ${{ env.APP_PORT }}
          VOLUME_DATA_DIR: ${{ env.VOLUME_DATA_DIR }}
        run: |
          ssh -o StrictHostKeyChecking=yes ${{ env.SSH_USER }}@${{ env.SSH_HOST }} << EOF
          set -e
          echo "Pulling image..."
          docker pull ${REGISTRY}/${IMAGE_NAME}:latest

          docker rm -f ${CONTAINER_NAME} >/dev/null 2>&1 || true

          mkdir -p ${VOLUME_DATA_DIR}

          docker run -d --name ${CONTAINER_NAME} \
            -p ${APP_PORT}:8080 \
            -v ${VOLUME_DATA_DIR}:${VOLUME_DATA_DIR} \
            --restart unless-stopped \
            ${REGISTRY}/${IMAGE_NAME}:latest

          echo "Verifying container status..."
          docker ps --filter "name=${CONTAINER_NAME}"
          EOF

      - name: Remote deploy over SSH (password option)
        if: ${{ steps.decide.outputs.use_password == 'true' }}
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          script_stop: true
          script: |
            set -e
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            docker rm -f ${{ env.CONTAINER_NAME }} >/dev/null 2>&1 || true
            mkdir -p ${{ env.VOLUME_DATA_DIR }}
            docker run -d --name ${{ env.CONTAINER_NAME }} \
              -p ${{ env.APP_PORT }}:8080 \
              -v ${{ env.VOLUME_DATA_DIR }}:${{ env.VOLUME_DATA_DIR }} \
              --restart unless-stopped \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            docker ps --filter "name=${{ env.CONTAINER_NAME }}"
