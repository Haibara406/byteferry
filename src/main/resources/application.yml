spring:
  application:
    name: ByteFerry

  # Redis
  data:
    redis:
      host: 8.156.75.132
      port: 6379
      password: "Ww249260523.."
      database: 2
      timeout: 5000
      lettuce:
        pool:
          max-active: 8
          max-idle: 4
          min-idle: 2

  # MySQL
  datasource:
    url: jdbc:mysql://8.156.75.132:3306/byteferry?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: "Ww249260523.."
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 20000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  # File upload
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

# Expose error messages in API responses
server:
  error:
    include-message: always

# MinIO
minio:
  endpoint: http://154.94.235.178:9000
  url: https://minio.haikari.top
  publicUrl: http://154.94.235.178:9000
  accessKey: haibara
  secretKey: "Ww249260523.."
  bucketName: byteferry

# Mail (SMTP)
spring.mail:
  host: smtp.qq.com
  port: 587
  username: ${MAIL_USERNAME:haibaraiii@foxmail.com}
  password: ${MAIL_PASSWORD:uoptzqxynsjvbijj}
  properties:
    mail.smtp.auth: true
    mail.smtp.starttls.enable: true

# ByteFerry custom config
byteferry:
  storage:
    file-dir: ${user.home}/.byteferry/files/
  share:
    default-expire-seconds: 600
    code-length: 6
  session:
    default-expire-seconds: 1800
  jwt:
    secret: ByteFerrySecretKey2026ForJwtTokenGenerationAndValidation
    expiration-hours: 72